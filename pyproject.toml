[project]
name = "bootdev-scheduling"
version = "0.1.0"
description = ""
authors = [
    {name = "andrew.wren",email = "awren.eclipse@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ortools (>=9.12.4544,<10.0.0)"
]

[tool.poetry]

[tool.poetry.group.dev.dependencies]
black = "^25.1.0"
pytest = "^8.3.5"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
